version: v2
modules:
  - path: .
    excludes:
      - .buf-cache
      - .uv-cache
      - .venv
deps:
  - buf.build/bufbuild/protovalidate
lint:
  use:
    - STANDARD
  except:
    - PACKAGE_DIRECTORY_MATCH
    - PACKAGE_VERSION_SUFFIX
breaking:
  use:
    - FILE
